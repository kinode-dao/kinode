[workspace]
resolver = "2"
members = [
    "app-store",
    "chain",
    "download",
    "downloads",
    "ft-worker",
    "install",
    "uninstall",
    "reset-store",
]

[profile.release]
panic = "abort"
opt-level = "s"
lto = true
